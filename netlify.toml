[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# SPA: 로그인 리다이렉트 후 /login 등 모든 경로가 index.html로 처리되도록
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
